# Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# For Local Development:
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=employee_management_db

# For a hosted MySQL provider, DATABASE_URL takes precedence over DB_*:
# DATABASE_URL=mysql://user:password@host:port/database
# DB_SSL=true

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# CORS Configuration (for frontend)
CORS_ORIGIN=http://localhost:3000,https://doantotnghiep253.vercel.app

# API Configuration
API_URL=http://localhost:5000

# For deployment, configure these variables in the hosting provider dashboard.
